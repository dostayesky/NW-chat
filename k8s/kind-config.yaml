# kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:
  # Port ของ API Gateway
  - containerPort: 30080 # 👈 1. นี่คือ NodePort (ต้อง 30k+)
    hostPort: 8080       # 👈 2. นี่คือ localhost ของคุณ (ตั้งเป็น 8080 ได้)
  
  # (Port ของ pgAdmin และ Postgres ก็ตั้ง hostPort เป็น 5051 และ 5433 ได้เช่นกัน)
  - containerPort: 30051
    hostPort: 5051
  - containerPort: 30033
    hostPort: 5433