apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      
      # ==========================================
      # 👇 1. Init Container (รัน "setup")
      # ==========================================
      initContainers:
      - name: setup-user-service
        image: connext-user-service:latest
        imagePullPolicy: IfNotPresent
        command: ["/server", "setup"] # 👈 ชื่อเก่าคือ build
        
        # 👇 ดึง ENV ทั้งหมดที่ "setup" ต้องการ
        envFrom:
        - configMapRef:
            name: app-config
        - secretRef:
            name: app-secret

      # ==========================================
      # 👇 2. Container หลัก (รัน "serve")
      # ==========================================
      containers:
      - name: user-service-container
        image: connext-user-service:latest
        imagePullPolicy: IfNotPresent
        # (รัน CMD ["serve"] อัตโนมัติจาก Dockerfile)
        ports:
        - containerPort: 8081 # 👈 Port จาก USER_ADDR
        
        # 👇 ดึง ENV ทั้งหมดที่ "serve" ต้องการ
        envFrom:
        - configMapRef:
            name: app-config
        - secretRef:
            name: app-secret
        # 
        # 👇 2. [นี่คือส่วนที่แก้ไข]
        #    เขียนทับ USER_ADDR ให้เป็น :8081 (เปิดฟังทุก interface)
        #
        env:
        - name: USER_ADDR
          value: ":8081"